# Roadmap

## Planned features

- Add support for non-csv data file formats
- Add JSON serialization and deserialization to `Body`, `Interaction`, and `System`

## Features under consideration

- Extend `System` to allow Frame analytes
- Extend `Field` to allow orientation-picking
- Add helper methods to `Interaction` to generate default parameter sets
